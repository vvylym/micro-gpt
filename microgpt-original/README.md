# microgpt-original

Minimal GPT implementation: scalar autograd, transformer forward, training, and inference.

Run with default data path:

```bash
cargo run -p microgpt-original
```

Or set `MICROGPT_INPUT_PATH` to use another file (one document per line).
